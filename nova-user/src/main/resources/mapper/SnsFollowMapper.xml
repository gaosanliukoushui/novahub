<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novahub.user.mapper.SnsFollowMapper">

    <select id="selectFollowerIds" resultType="java.lang.Long">
        SELECT user_id FROM sns_follow WHERE follow_id = #{userId}
    </select>

    <select id="selectFollowingIds" resultType="java.lang.Long">
        SELECT follow_id FROM sns_follow WHERE user_id = #{userId}
    </select>

    <select id="countFollowers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sns_follow WHERE follow_id = #{userId}
    </select>

    <select id="countFollowings" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sns_follow WHERE user_id = #{userId}
    </select>
</mapper>
