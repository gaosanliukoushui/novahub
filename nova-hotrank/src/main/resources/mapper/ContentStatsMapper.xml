<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novahub.hotrank.mapper.ContentStatsMapper">

    <resultMap id="BaseResultMap" type="com.novahub.hotrank.entity.ContentStats">
        <id column="id" property="id"/>
        <result column="content_id" property="contentId"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="like_count" property="likeCount"/>
        <result column="collect_count" property="collectCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="view_count" property="viewCount"/>
        <result column="heat_score" property="heatScore"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByContentId" resultMap="BaseResultMap">
        SELECT * FROM content_stats WHERE content_id = #{contentId}
    </select>

    <select id="selectTopByHeatScore" resultMap="BaseResultMap">
        SELECT * FROM content_stats
        ORDER BY heat_score DESC
        LIMIT #{limit}
    </select>

    <select id="selectTopByTypeAndHeatScore" resultMap="BaseResultMap">
        SELECT * FROM content_stats
        WHERE type = #{contentType}
        ORDER BY heat_score DESC
        LIMIT #{limit}
    </select>

    <insert id="upsert" parameterType="com.novahub.hotrank.entity.ContentStats">
        INSERT INTO content_stats (id, content_id, user_id, type, like_count, collect_count, comment_count, view_count, heat_score, last_update_time, create_time)
        VALUES (#{id}, #{contentId}, #{userId}, #{type}, #{likeCount}, #{collectCount}, #{commentCount}, #{viewCount}, #{heatScore}, #{lastUpdateTime}, #{createTime})
        ON DUPLICATE KEY UPDATE
            like_count = VALUES(like_count),
            collect_count = VALUES(collect_count),
            comment_count = VALUES(comment_count),
            view_count = VALUES(view_count),
            heat_score = VALUES(heat_score),
            last_update_time = VALUES(last_update_time)
    </insert>
</mapper>
