spring:
  datasource:
    url: jdbc:mysql://test-db:3306/nova_hub?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    password: test123
  data:
    redis:
      host: test-redis
      port: 6379
      password: test-redis-pass
  kafka:
    bootstrap-servers: test-kafka:9092
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

minio:
  endpoint: http://test-minio:9000

elasticsearch:
  host: test-es
  port: 9200

logging:
  level:
    com.novahub: INFO
    com.novahub.web: WARN
